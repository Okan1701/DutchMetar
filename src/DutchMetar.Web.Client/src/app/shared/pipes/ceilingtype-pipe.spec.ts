import { CeilingtypePipe } from './ceilingtype-pipe';

describe('CeilingtypePipe', () => {
    it('create an instance', () => {
        const pipe = new CeilingtypePipe();
        expect(pipe).toBeTruthy();
    });
});
